Implement close/shutdown select and poll. Poll is called WSAPoll on windows.
Implement the error system.
Consider exposing the c integer types in thincrust. Not sure if there is a point not doing so. If they are variable on the underlying architecture, they should be presented as such.
